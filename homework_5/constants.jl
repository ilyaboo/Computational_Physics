const Lx::Float64 = 5.0   # width of semiconductor in nm
const Ly::Float64 = 10.0   # length of semiconductor in nm
const a::Float64 = 1.0   # length of the low potential areas in nm
const b::Float64 = 0.5   # width of the low potential areas in nm
const x0::Float64 = 2.0   # distance from low potential are to the longer side of the demiconductor in nm
const y0::Float64 = 2.0   # distance from low potential are to the shorter side of the demiconductor in nm
const V0::Float64 = -0.1   # potential value
const β::Float64 = 13.11   # constant from Schrodinger eqaution in 1/(eV*nm^2)